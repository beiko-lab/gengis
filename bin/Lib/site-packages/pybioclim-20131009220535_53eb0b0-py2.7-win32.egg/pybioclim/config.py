import os

# get the path to the package data directory
_ROOT = os.path.abspath(os.path.dirname(__file__))
_ADDITIONAL = os.getcwd()
def get_data_dir():
    return os.path.join(_ROOT, 'data')
def get_additional_data_dir():
	path = os.path.join(_ADDITIONAL,'..\..\..\plugins\WorldClimQuery\data')
	return path 

	
DATA_DIR = get_data_dir()
DATA_ADDIT = get_additional_data_dir()
DATA_PATHS = ('.', DATA_DIR,)
if os.path.exists(DATA_ADDIT):
	DATA_PATHS = ('.', DATA_DIR, DATA_ADDIT,)

def find_data(name):
    for data_dir in DATA_PATHS:
		path = os.path.join(data_dir, name)
		if os.path.exists(path):
			return path
